# Datenbank-Konfiguration
# Für lokale Entwicklung: SQLite verwenden (Standard)
DB_TYPE=sqlite
DB_PATH=salon_crm.db

# Für Cloud-Deployment: PostgreSQL verwenden
# DB_TYPE=postgresql
# DATABASE_URL=postgresql://username:password@host:port/database

# Beispiel für Supabase:
# DATABASE_URL=postgresql://postgres:your_password@db.xxxxx.supabase.co:5432/postgres

# Beispiel für Neon:
# DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb
